# 2026-05-03 工作日志

## 借记卡模块 - 华瑞银行
- 新建表 `debit_card_transactions`（借贷记结构，含对手信息）
- 写导入脚本 `scripts/import_huarui.py`，从 MinerU 导出的 full.md 解析 HTML 表格
- 成功导入 **1710 条** 交易（2024-04-01 ~ 2025-03-13）
- 总收 ¥2.48亿，总付 ¥3.90亿（资金划转账户）

## OCR 数据质控
- 导出 `hrb_issues.csv`（265条待确认行）
- 发现 3 类 OCR 问题：
  | 问题类型 | 数量 | 表现 |
  |---|---|---|
  | 金额入描述 | 61条 | PDF的金额列被OCR读到描述列 |
  | 借贷相等小计行 | 202条 | PDF分页小计被OCR当交易 |
  | 借方是日期 | 3条 | 日期值被读入借方列 |
- 建议用户重点核对 PDF：5月中下旬、6月、9-12月

## git 待推送
- 新表 + 导入脚本 + CSV质控报告

## 信用卡账单解析器大修（对比验证）
拿到吴老板整理的Excel标准化数据后，逐银行对比PG数据，发现大量问题并修复。

### 通用修复
- **唯一约束改版**: `(bank_code, trans_date, post_date, card_last4, description, amount)` → `(..., raw_line_text)` + `ON CONFLICT ON CONSTRAINT uq_txn`
- **loader.js**: raw_line_text追加导入序号`|${i}`，优先用parser的trans_type
- **decodeEmail修复**: CTE检测范围扩大到Content-Type前300字符（解决部分邮件CTE在CT前面的问题）

### 光大银行 CEB
- 修复base64解码（CTE在Content-Type前面的情况）
- ceb.js: 加卡号提取（之前全空）、加trans_type、加纯正数匹配模式
- 结果: 2603 8条完美匹配✅

### 民生银行 CMBC
- 搜索"交易明细"改为"交易日 SOLD"（早期账单用不同表头）
- 描述正则 `[^\d]+?` → `.+?`（银联入账类描述含数字）
- 去掉parser内`seen`去重（同金额合法交易被吞）
- Excel日期序列号公式修正: 去掉多余的`-1`修正
- **结果: 2602/2603/2604 三张表100%匹配✅**（16+34+21=71条）

### 已对比的银行汇总（全部通关）
| 银行 | 验证结果 | 修复要点 |
|------|---------|---------|
| 民生 CMBC | 3张表100%✅ | 交易日SOLD标签、描述正则、seen移除、序列号公式 |
| 浙商 CZB | 5张表100%✅ | 纯数字金额、3字描述、trans_type |
| 中信 CITIC | 25/25✅ | 纯文本行、第二CNY金额 |
| 交行 BOCOM | 3张表100%✅ | Credit/Debit两段、还款负消费正 |
| 工行 ICBC | 62/62✅ | 文本行、存入/支出方向、加1465卡 |
| 农行 ABC | 2张+持卡人✅ | 8761→吴大军映射 |
| 建行 CCB | 3张表100%✅ | 多卡持卡人映射 |
| 招行 CMB | 35/35✅ | 分段落+多行分组、卡号0696前导0 |
| 光大 CEB | 2603✅ | 卡号提取、base64解码 |
| 广发 CGB | 3张81条100%✅ | 独立解析器、去DEPOSIT、(赠送)→REFUND |
| 平安 PAB | 3张13条100%✅ | `&yen;`正则修复、卡号列缺失确认 |
| 浦发 SPDB | 通用修复覆盖 | PDF/Excel格式无需验证 |
| 中行 BOC | 通用修复覆盖 | PDF附件格式无需验证 |

## Git推送 + PG清空重导
- **git commit**: `修复农行解析: m[11]入账金额含符号位 + null类型描述推断` (abc.js)
- **git push**: 两个commit推送至 github ✅
- **PG清空重导**: TRUNCATE后逐银行重新导入所有12家银行
- **BOC首次入库成功**: 97条交易（之前PG中没有中行数据）
- **最终状态**: 2008条交易, 77个账单, 12家银行
- **农行金额方向确认**: SPEND全正 / REFUND全正（bug）→ 修正为全负 / REPAY全负 ✅
- **⚠️ REFUND方向修正**: abc.js去掉REFUND保留原始值特例，全部统一取反 `amount = -settleAmt`

## 账期改为按账单ID筛选（非自然月）
用户反馈账期应按"一期邮件=一期账单"筛选，而非自然月。修改内容：
- **后端** `/api/bill-cycles`: 改为返回实际账单列表(bills)含id/label/bank_code/cycle_start等信息（非月份列表）
- **后端** `/search`: 新增 `bill_id` 查询参数，`build_where_clause` 支持 bill_id 精确匹配
- **后端** `search` SQL: SELECT 增加 `t.bill_id` 字段
- **后端** `models.py`: TransactionItem 加 bill_id 字段
- **前端**: 4×4月份宫格 → 可滚动账单列表，按银行分组+彩色圆点标识，点击按bill_id搜索
- bill_cycle自然月查询保留作兼容层

## 新增日历视图
- **后端**: 新增 `/api/daily` 端点，复用 search 筛选条件，按日分组汇总支出(spend)和收入(repay)
- **前端**: 结果区增加"📅 日历"切换按钮，点击后隐藏表格显示大日历
- 日历格子: 日期号 + 红色柱(支出) + 绿色柱(还款) + 红绿金额数字
- 月份导航: 左右箭头切换月份，自动定位到最新数据月份

## 宁波银行NBC导入
- 桌面36个PDF→MinerU OCR→scripts/import_nbc.py→入库PG
- 36个账单, 1659条交易, 卡号****7108, 持卡人吴华辉
- 数据跨度: 2023-06 ~ 2026-05
- 已加到BANK_NAMES和前端银行列表

